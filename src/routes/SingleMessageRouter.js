import React, { Component } from 'react';
import SingleMessage from '../components/SingleMessage'

class SingleMessageRouter extends Component {

    render() {
        return(
            <SingleMessage/>
        )
    }
}

export default SingleMessageRouter;
